---
description: Diagnose Premind's Claude Code plugin runtime.
---

# Premind doctor

Use the `mcp__plugin_premind_premind__probe` tool. Explain that v0.2 delivers reminders only at a Stop boundary, cannot wake an otherwise inactive Claude session, and treats `idleDeliveryThresholdMs` as OpenCode-only.
