/**
 * Pi package entrypoint for premind.
 *
 * Phase 1 intentionally keeps this extension as a no-op scaffold so the
 * package can be discovered by Pi before the Pi-native integration is ported.
 */
export default function premindPiExtension(_pi: unknown): void {
  // Pi integration will be implemented in phases. See PI_PLAN.md.
}
